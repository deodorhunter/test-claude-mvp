# AI Reference — AI Platform MVP
> Auto-generated by /init-ai-reference on 2026-03-29.
> Re-run `/init-ai-reference` if the stack changes significantly.
> Claude reads this file instead of exploring the codebase.

> For delegation patterns and model routing, see `docs/ORCHESTRATION_GUIDE.md`.

## Stack

| Layer | Technology | Version (if known) |
|---|---|---|
| Backend language | Python | 3.11+ |
| Backend framework | FastAPI | >=0.115.0 |
| ORM / DB client | SQLAlchemy + asyncpg | >=2.0.0 / >=0.29.0 |
| Database | PostgreSQL | 16-alpine |
| Cache / Queue | Redis | 7-alpine |
| Vector Database | Qdrant | v1.9.2 |
| AI Models (local) | Ollama | latest |
| AI Models (cloud) | Anthropic Claude API | >=0.25.0 |
| CMS Backend | Plone | 6.0.12 |
| Frontend Framework | Volto (React-based) | 18.0.0 |
| Frontend Build | Razzle | (part of Volto image) |
| Test runner (backend) | pytest | >=8.0.0 |
| Test runner (async) | pytest-asyncio | >=0.23.0 |
| Container orchestration | Docker Compose | 3.9 |
| Web server (ASGI) | Uvicorn | >=0.30.0 |

## Services & Ports

| Service | Port | Notes |
|---|---|---|
| API (FastAPI) | 8000 | Backend REST API; container: ai-platform-api |
| PostgreSQL | 5432 | Primary data store; container: ai-platform-postgres |
| Redis | 6379 | Cache + rate limiting; container: ai-platform-redis |
| Qdrant | 6333 | Vector DB for embeddings/RAG; container: ai-platform-qdrant |
| Ollama | 11434 | Local LLM inference; container: ai-platform-ollama |
| LiteLLM proxy | 4000 | OpenAI-compatible proxy for Ollama; see `infra/docker-compose.ai-tools.yml` |
| Serena MCP | 9121 | LSP code navigation (Docker mode); see `infra/docker-compose.ai-tools.yml` |
| Plone (CMS) | 8080 | REST API backend; container: ai-platform-plone |
| plone-mcp (Node.js) | 9120 | Self-hosted MCP server for Plone; SSE transport; container: ai-platform-plone-mcp |
| Volto (Frontend) | 3000 | React frontend; container: ai-platform-volto |

## Make Targets

| Target | What it does |
|---|---|
| `make up` | Start all Docker services with rebuild (`docker compose up -d --build`) |
| `make down` | Stop all Docker services (`docker compose down`) |
| `make logs` | Follow logs from all services (`docker compose logs -f`) |
| `make migrate` | Run pending Alembic migrations (`docker compose exec api alembic upgrade head`) |
| `make shell-api` | Open bash shell in API container |
| `make shell-postgres` | Open psql shell in PostgreSQL container |

## Test Commands

```bash
# Run all backend unit tests
pytest -q --tb=short

# Run a single test file
pytest tests/test_planner.py -q --tb=short

# Run with coverage
pytest --cov=app tests/

# Run integration tests against live Docker environment
# (requires docker compose up)
docker compose exec ai-platform-api pytest tests/integration/ -q

# Run E2E tests (Phase 3+)
# npm run test:e2e (in Volto container or locally if Node installed)
```

## Key File Paths

| Purpose | Path |
|---|---|
| API entry point | `backend/app/main.py` |
| Main config / settings | `backend/app/config.py` |
| DB models | `backend/app/db/models.py` |
| DB migrations directory | `backend/alembic/versions/` |
| Auth module | `backend/app/auth/` |
| API routes (endpoints) | `backend/app/api/` |
| Docker Compose (local) | `infra/docker-compose.yml` |
| Environment variable template | `.env.example` |
| Dockerfile (backend) | `infra/docker/Dockerfile.backend` |
| AI/ML modules | `ai/` |
| Planner module | `ai/planner/planner.py` |
| Model adapters | `ai/models/` |
| MCP registry + allowlist | `ai/mcp/registry.py` |
| Plone MCP Python adapter | `ai/mcp/servers/plone.py` |
| plone-mcp Node.js source | `infra/plone-mcp/src/index.ts` (upstream + SSE patch) |
| Plugin manager | `plugins/` |

## Environment Variables

```
DATABASE_URL — PostgreSQL async connection (postgresql+asyncpg://)
REDIS_URL — Redis cache + rate limiting
SECRET_KEY — JWT signing key
PLONE_BASE_URL — Plone CMS base URL
QDRANT_URL — Qdrant vector DB URL
CORS_ORIGINS — Allowed origins (JSON array)
ENVIRONMENT — "development" | "production"
ACCESS_TOKEN_EXPIRE_MINUTES — JWT access token TTL (default: 60)
REFRESH_TOKEN_EXPIRE_DAYS — JWT refresh token TTL (default: 7)
AI_MODE — "demo" (Ollama) or production
OLLAMA_URL — Ollama endpoint
OLLAMA_MODEL — Model name
ANTHROPIC_BASE_URL — Anthropic API endpoint override (LiteLLM: http://localhost:4000)
CONTEXT7_API_KEY — Context7 MCP (Upstash)
POSTGRES_DB / POSTGRES_USER / POSTGRES_PASSWORD — DB credentials
PLONE_USERNAME / PLONE_PASSWORD — Plone admin credentials
PLONE_MCP_URL — plone-mcp endpoint
```

## Health Check Endpoints

| Service | Endpoint | Expected response | Container health |
|---|---|---|---|
| API (FastAPI) | `GET http://localhost:8000/health` | `200 OK` | Port 8000 |
| PostgreSQL | Built-in: `pg_isready` | psql responsive | Port 5432 |
| Redis | Built-in: `redis-cli ping` | PONG | Port 6379 |
| Qdrant | `GET http://localhost:6333/healthz` | `200 OK` | Port 6333 |
| Ollama | `GET http://localhost:11434/api/tags` | JSON list of models | Port 11434 |
| Plone | `GET http://localhost:8080/@@ok` | OK page | Port 8080 |
| plone-mcp | `GET http://localhost:9120/sse` | SSE stream opens | Port 9120 |
| Volto | `GET http://localhost:3000` | React app | Port 3000 |

## Critical Volume Mounts (Docker)

| Host Path | Container Path | Purpose |
|---|---|---|
| `../plugins/` | `/app/plugins/` | Plugin code (mounted in API) |
| `../ai/` | `/app/ai/` | AI/ML modules (mounted in API) |
| `backend/` (implied) | `/app/backend/` | Backend code (in Dockerfile) |
| Persistent volumes | See docker-compose.yml | Data persistence (postgres_data, redis_data, qdrant_data, ollama_data) |

## IDE & LSP Settings

**Serena config:** `infra/serena_config.json` (rule-019) — ignored: `.git/`, `node_modules/`, `__pycache__/`, `.venv/`. Prevents index.lock contention during worktree merges.

## MCP Servers

| Server | Transport | Endpoint | Scope | Status |
|---|---|---|---|---|
| context7 | stdio (npx) | mcp.context7.com (cloud) | Library docs | ✅ Verified |
| serena | SSE | http://localhost:9121/sse | Python + TypeScript LSP | ✅ Main session; sub-agent: pending verification |

Config file: `.mcp.json` at project root (project-level — loaded for all Claude Code sessions).  
Serena requires: `make up-ai-tools` (Docker). Python and TypeScript files only (`.serena/project.yml`).  
context7: `CONTEXT7_API_KEY` in env (optional for free tier). Receives library names + query strings only — never source code or schema (rule-011).  


## Persistent Memory

Location: `~/.claude/projects/-Users-martina-personal-projects-test-claude-mvp/memory/`  
Index: `MEMORY.md` (auto-loaded each session; 150-line limit).  
Types: `user`, `feedback`, `project`, `reference`.  
Not committed to repo. Managed automatically by Claude Code across sessions.  
Current entries: 15 active as of 2026-04-04 (incidents, feedback rules, MVP initiative, infrastructure).
