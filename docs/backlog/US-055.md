# US-055: Doc Verification CI — `make verify-docs`

**Agent:** QA Engineer
**Phase:** 3b (Automation & Cognitive Tooling)
**Dependencies:** —
**Priority:** high
**Status:** 📋 Backlog

## Context

User feedback: "hallucinations in docs and governance files, should be checked always and be uber cost efficient."
Internal observation I2: no automated verification for documentation accuracy. EUR-Lex links marked "[Verified]" but no CI validates them.

**Tool context:** Pure bash script — works identically in Claude Code terminal and Copilot terminal. No tool-specific dependency.

## Task

1. **Create** `benchmark/verify-docs.sh` that checks:
   - All HTTP(S) links in `docs/` resolve (curl -sI, HEAD request only)
   - Port numbers in `docs/AI_REFERENCE.md` match `infra/docker-compose.yml` port mappings
   - Make targets referenced in docs exist in `Makefile`
   - All US files referenced in `BACKLOG.md` exist on disk
   - Plugin status in `PLUGIN_MANIFEST.md` matches actual directory contents
   - Rule files in `project/` have required frontmatter fields (id, trigger, updated)
2. **Add** `make verify-docs` target to Makefile
3. **Keep it lightweight:** bash + curl + grep only, no heavy dependencies
4. **Exit 0** on success, non-zero with specific error messages on failure

## Acceptance Criteria

- [ ] `benchmark/verify-docs.sh` exists and is executable
- [ ] `make verify-docs` target works
- [ ] Script catches at least: broken links, missing US files, port mismatches
- [ ] Runs in <30 seconds on current repo
- [ ] Exit 0 on current repo state (fix any issues found)
- [ ] No Python or Node.js dependencies — pure bash

## Files Involved

```
benchmark/verify-docs.sh (new)
Makefile
```
