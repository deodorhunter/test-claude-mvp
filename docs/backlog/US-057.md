# US-057: Copy-First Adoption Path + HOW-TO-ADOPT Rewrite

**Agent:** Doc Writer
**Phase:** 3c (Adoption, DX & Multi-Client)
**Dependencies:** —
**Priority:** high
**Status:** 📋 Backlog

## Context

User feedback: "I read HOW-TO-ADOPT, but said fuck it and copied whole .claude folder and claude things directly. Also, I run machine level or npm -g installations of my MCPs." And: "feedback for this framework is clunky as hell."

The "copy .claude directly" pattern IS the adoption path. Document it as primary, not the exception. No `make init-framework` needed — that's over-engineering for `cp -r`.

## Task (Claude Code-first)

1. **Rewrite** `framework-template/HOW-TO-ADOPT.md`:
   - **PRIMARY path:** `cp -r .claude/ your-project/.claude/ && cp CLAUDE.md your-project/` — 2 commands, done
   - **SECONDARY path:** selective adoption (pick individual agents/rules)
   - Document global MCP install patterns: `npm install -g @anthropic/serena-mcp`, `pip install context7-mcp` (verify actual package names first)
   - Document `.github/copilot-instructions.md` copy for Copilot users
   - Post-copy checklist: update `docs/AI_REFERENCE.md` for your stack, remove project-specific rules
2. **Add lightweight feedback section** (absorbs US-059's valid kernel):
   - Add "Providing Feedback" section to `framework-template/HOW-TO-ADOPT.md` or project `README.md`
   - Format: simple markdown template (category, description, proposed fix) — 10 lines max
   - No `make feedback`, no YAML, no dedicated file
3. **Do NOT create** `make init-framework` target — `cp -r` is sufficient

## Acceptance Criteria

- [ ] HOW-TO-ADOPT.md primary path is `cp -r .claude/` (2-command adoption)
- [ ] Global MCP install patterns documented (package names verified, not hallucinated)
- [ ] Post-copy checklist included
- [ ] Lightweight feedback template present (≤10 lines)
- [ ] No Makefile changes

## Files Involved

```
framework-template/HOW-TO-ADOPT.md
```
