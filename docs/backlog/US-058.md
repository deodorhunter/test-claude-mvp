# US-058: Copilot-First Instructions — Standalone Rewrite

**Agent:** Doc Writer
**Phase:** 3c (Adoption, DX & Multi-Client)
**Dependencies:** US-053 (orchestration guide informs Copilot patterns)
**Priority:** high
**Status:** 📋 Backlog

## Context

User feedback: "Copilot VS Code is really bad with this framework" and "Copilot tooling uses regex a lot for reads, should we try an implementation here too?"

Current `.github/copilot-instructions.md` is a thin redirect to CLAUDE.md concepts. Copilot Chat operates differently from Claude Code — it has no phase gates, no sub-agents, no compress-state. Instructions must be standalone and Copilot-native.

**Implementation order:** Claude Code governance (CLAUDE.md, .claude/) is the source of truth. This US creates a Copilot-native PROJECTION of those rules — not a duplicate governance layer. Copilot instructions should reference Claude Code as authority for Speed 2 work.

## Task

1. **Rewrite** `.github/copilot-instructions.md` as a **standalone** document:
   - Remove all references to Claude Code concepts (Speed 2, phase gates, compress-state)
   - Define Speed 1 patterns specifically for Copilot Chat
   - Document Copilot's tool capabilities: regex search, file reading, terminal
   - Add Serena MCP patterns specific to Copilot (tool_search_tool_regex → load → call)
   - Document limitations: "Copilot cannot do X, use Claude Code for Y"
2. **Update** `.github/instructions/*.instructions.md` files:
   - Ensure they work independently without CLAUDE.md context
   - Add Copilot-specific tips where applicable
3. **Evaluate** regex-based reads:
   - Document when `tool_search_tool_regex` + targeted reads is more cost-efficient than full file reads
   - Add guidance to instruction files if beneficial
4. **Test** with 3 representative Speed 1 tasks in Copilot Chat

## Acceptance Criteria

- [ ] `copilot-instructions.md` is standalone — no CLAUDE.md dependency
- [ ] Copilot-specific tool patterns documented (regex search, Serena via MCP)
- [ ] Limitations section: what requires Claude Code
- [ ] 3 Speed 1 tasks work correctly in Copilot without file exploration beyond attachments
- [ ] `.github/instructions/` files work independently

## Files Involved

```
.github/copilot-instructions.md
.github/instructions/*.instructions.md
```
