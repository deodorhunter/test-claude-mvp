# US-051: Model Routing Strategy — Haiku/Sonnet/Local Decision Matrix

**Agent:** Doc Writer
**Phase:** 3a (Token Optimization)
**Dependencies:** —
**Priority:** critical
**Status:** 📋 Backlog

## Context

User feedback: "too much Sonnet usage, more Haiku?" — no documented strategy for which model tier to use per task type.

**Verified mechanism:** Claude Code agents support a `model:` key in their `.md` frontmatter. Already used in 12 agent files (e.g., doc-writer and qa-engineer use `claude-haiku-4-5-20251001`). The product-owner agent already defines a Task Complexity Matrix (LOW→Haiku, MEDIUM/HIGH→Sonnet, FULL-CODEBASE→Opus).

**What does NOT exist (verified):** No global `smallFastModel` settings key. No programmatic cost extraction API for session logs. GitHub Models API has no Claude Code adapter — do NOT attempt integration.

## Task (Claude Code-first)

1. **Audit all 12+ agent `.md` frontmatter** — verify each agent's `model:` key aligns with the Task Complexity Matrix in product-owner.md:
   - doc-writer, qa-engineer Mode A, critic → should be `claude-haiku-4-5-20251001`
   - implementing agents (backend-dev, security-engineer, aiml-engineer) → should be `claude-sonnet-4-6`
   - orchestrator → `claude-sonnet-4-6` (or `dynamic` if variable complexity)
   - List any mismatches and correct them
2. **Add "Model Routing" section** to `docs/AI_REFERENCE.md`:
   - Reference the product-owner Task Complexity Matrix as source of truth
   - Document per-agent frontmatter `model:` as the ONLY routing mechanism
   - Document GitHub Models limitation (no Claude Code adapter)
   - Document Copilot limitation (model selection is subscription-tier, no per-task routing)
3. **Update** `.claude/agents/orchestrator.md` routing table — add model tier column

## Acceptance Criteria

- [ ] `docs/AI_REFERENCE.md` contains "Model Routing" section referencing Task Complexity Matrix
- [ ] All agent `.md` frontmatter `model:` keys audited (≥10 agents), mismatches corrected
- [ ] `.claude/agents/orchestrator.md` routing table includes model tier column
- [ ] GitHub Models + Copilot limitations documented
- [ ] No hallucinated settings keys or CLI flags introduced

## Files Involved

```
docs/AI_REFERENCE.md
.claude/agents/orchestrator.md
.claude/agents/*.md (audit all frontmatter model: keys)
```
