# US-064: SWE-Agent Evaluation — Go/No-Go Recommendation

**Agent:** QA Engineer
**Phase:** 3d (Infrastructure & Architecture)
**Dependencies:** —
**Priority:** medium
**Status:** 📋 Backlog

## Context

User feedback: "benchmarking is not good, probably should use https://github.com/SWE-agent/mini-swe-agent or something similar."

Current benchmarks (`benchmark/`) measure static quantities (context size, injection ratio, framework overhead). They don't measure task completion quality or cost-per-task. Full benchmark harness is premature — first determine if mini-swe-agent is viable.

## Task (Claude Code-first, evaluation only)

1. **Research** mini-swe-agent (https://github.com/SWE-agent/mini-swe-agent):
   - Read README, setup docs, dependencies, runtime requirements
   - Can it run against a Claude Code-governed codebase?
   - Does it support custom task definitions or only SWE-bench?
   - What model providers does it support?
   - License, maintenance status, community activity
2. **Evaluate alternatives** (brief):
   - SWE-bench-lite: applicable to governance framework testing?
   - Custom harness: what would minimal viable benchmark look like?
3. **Write evaluation report:** `benchmark/SWE_AGENT_EVALUATION.md`
   - Feasibility assessment for each option
   - Go/no-go recommendation with reasoning
   - If go: estimated effort for Phase 4 implementation US
   - If no-go: what alternative approach for task-quality benchmarking

**Scope boundary:** Do NOT build benchmark tasks, runner scripts, verification harnesses, or comparison data. This US produces a recommendation document only. Implementation is a separate US if go.

## Acceptance Criteria

- [ ] `benchmark/SWE_AGENT_EVALUATION.md` exists with go/no-go recommendation
- [ ] mini-swe-agent feasibility assessed (setup, compatibility, model support)
- [ ] At least 1 alternative evaluated
- [ ] No benchmark runner code created (evaluation only)

## Files Involved

```
benchmark/SWE_AGENT_EVALUATION.md (new — evaluation report only)
```
