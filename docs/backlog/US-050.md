# US-050: Archive Rules Cleanup — Move Out of Claude Code Auto-Load Path

**Agent:** DevOps/Infra
**Phase:** 3a (Token Optimization)
**Dependencies:** —
**Priority:** critical
**Status:** ✅ Done

## Context

Claude Code auto-loads ALL `.md` files from `.claude/rules/` subdirectories — including `archive/`. The 12 archived rules (418 lines) are verbose originals superseded by compact `project/` versions (289 lines). Both load every session — 59% redundant token cost.

User feedback: "Too much token usage" (×3 reports).
Internal observation I1: archived rules load ~3,600 tokens/session unnecessarily.

**Tool context:** This is a Claude Code structural change. Copilot uses `.github/instructions/` which separately references some archive rules — those refs must also be cleaned.

## Task (Claude Code-first)

1. `mv .claude/rules/archive/ docs/rules-archive/` — moves outside Claude Code scan path
2. Update `.claude/rules/README.md` to note archive location
3. Update `CLAUDE.md` part_3 to remove all `@.claude/rules/archive/` references
4. Audit `.github/instructions/*.instructions.md` — remove any `<instruction>` entries pointing to `.claude/rules/archive/`
5. Verify: `find .claude/rules -name '*.md' | xargs wc -l` before/after — expect ≥400 line reduction
6. Smoke test: start new Claude Code session, confirm no archive rules in context

## Acceptance Criteria

- [ ] `.claude/rules/archive/` no longer exists
- [ ] `docs/rules-archive/` contains all 12 original verbose rules (audit trail preserved)
- [ ] `CLAUDE.md` part_3 references only `project/` rules
- [ ] No `.github/instructions/` file references archive rules
- [ ] Claude Code context reduced by ≥3,000 tokens/session

## Files Involved

```
.claude/rules/archive/ → docs/rules-archive/
.claude/rules/README.md
CLAUDE.md
.github/instructions/*.instructions.md
```
