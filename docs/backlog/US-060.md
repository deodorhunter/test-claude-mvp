# US-060: Framework Template Sync with Current Agent State

**Agent:** DevOps/Infra
**Phase:** 3c (Adoption, DX & Multi-Client)
**Dependencies:** US-050 (archive cleanup should be done first)
**Priority:** medium
**Status:** 📋 Backlog

## Context

Internal observation I5: `framework-template/` has diverged from current `.claude/agents/` state. Missing agents (debugger, critic, frontend-dev) not in template. Template rules don't match compact project/ versions.

## Task

1. **Audit** differences between `framework-template/` and current `.claude/`:
   - Missing agent files
   - Outdated rule templates
   - Missing instruction files
2. **Sync** template to match current state:
   - All agent `.md` files from `.claude/agents/` → `framework-template/agents/`
   - All project rule files → `framework-template/rules/`
   - Template versions of `CLAUDE.md` and `copilot-instructions.md`
3. **Add** version stamp to template files:
   - `<!-- framework-template v3.0 | synced: 2026-04-XX -->` in each file header
4. **Verify** `make init-framework` (US-057) uses the synced template

## Acceptance Criteria

- [ ] All current agents represented in `framework-template/agents/`
- [ ] All active project rules in `framework-template/rules/`
- [ ] Version stamps on all template files
- [ ] No orphan files in template (nothing that doesn't exist in `.claude/`)
- [ ] Template README updated with sync date

## Files Involved

```
framework-template/agents/
framework-template/rules/
framework-template/CLAUDE.template.md
framework-template/copilot-instructions.template.md
```
