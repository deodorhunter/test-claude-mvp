# US-011: Plugin Runtime — Subprocess Isolation + Resource Limits

**Agente:** Security Engineer
**Fase:** 2a
**Dipendenze:** US-010
**Priorità:** critical
**Stato:** 📋 Backlog

## Contesto

I plugin vengono eseguiti in subprocess isolati per prevenire che un plugin malfunzionante o compromesso possa accedere ai dati di altri tenant o all'host. Comunicazione via JSON su stdin/stdout. Un pool di subprocess per coppia `(tenant_id, plugin_name)`.

**Eccezione MVP:** `plone_integration` ha accesso di rete a Plone — tutti gli altri plugin hanno rete bloccata.

## Task

Implementare `PluginRuntime` in `backend/app/plugins/runtime.py`:
- Esecuzione plugin in subprocess con timeout 10s
- Limiti CPU/memoria tramite resource limits (cgroups se disponibili, fallback `resource` module)
- Comunicazione JSON su stdin/stdout
- Pool di subprocess per `(tenant_id, plugin_name)` con max 2 worker per coppia
- Network access bloccato per tutti i plugin eccetto `plone_integration`

## Acceptance Criteria

- [ ] Plugin eseguito in subprocess separato (non in-process)
- [ ] Timeout 10s: plugin che supera il limite viene killato e ritorna errore
- [ ] Limite memoria applicato (minimo: raise se non supportato dalla piattaforma, non silenzioso)
- [ ] Input/output solo via JSON su stdin/stdout — nessun altro canale
- [ ] Plugin di tenant A non può leggere dati di tenant B (verifica via test)
- [ ] `plone_integration` può fare richieste HTTP verso Plone, altri plugin no
- [ ] Unit test: timeout enforcement, isolation cross-tenant, JSON protocol
- [ ] Security Notes in `docs/progress/US-011-done.md` con residual risks espliciti

## File coinvolti

```
backend/app/plugins/runtime.py      # PluginRuntime (creare)
backend/app/plugins/manager.py      # integrazione con runtime
backend/tests/test_plugin_runtime.py
docs/progress/US-011-done.md
```

## Output atteso

- `PluginRuntime` class con subprocess isolation funzionante
- Test che dimostrano isolation e timeout enforcement
- Security Notes con qualsiasi assunzione o rischio residuo
